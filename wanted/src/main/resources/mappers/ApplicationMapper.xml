<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.wanted.mapper.ApplicationMapper">
	<insert id="insertApplication">
		insert into Application (member_id, hiring_id)
		values (#{member_id},#{hiring_id})
	</insert>
</mapper>